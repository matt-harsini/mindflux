import { User } from "../models/userModel.js";

function login() {
  console.log(123);
  res.json({ msg: "Login User" });
}

function register() {
  res.json({ msg: "Login User" });
}

export { login, register };
